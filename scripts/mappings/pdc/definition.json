{
  "changemaker": {
    "name": "{{organization_name}}"
  },
  "fields": [
    {
      "baseFieldShortCode": "{{code}}",
      "type": "string",
      "value": "{{value}}",
      "batch": {
        "notes": "{{source}}",
        "source": {
          "label": "TechSoup",
          "dataProvider": {
            "name": "TechSoup",
            "shortCode": "techsoup"
          },
          "dataProviderShortCode": "techsoup"
        }
      }
    }
  ]
}
